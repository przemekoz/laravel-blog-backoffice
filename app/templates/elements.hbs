<Wrapper::List
  @modelName="element"
  @filterType={{this.filterType}}
  @sortOptions={{this.sortOptions}}
  @meta={{@model.meta}}
  @data={{@model.data}}

  @header={{lang "List of Elements"}}
  @createLabel={{lang "Add Element"}}
  @createRoute="element-create"

/>
