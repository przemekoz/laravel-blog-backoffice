
<Wrapper::Card @header="List of Elements">

  <Wrapper::List
    @modelName="element"
    @filterType={{this.filterType}}
    @sortOptions={{this.sortOptions}}
    @meta={{@model.meta}}
    @data={{@model.data}}
  />

</Wrapper::Card>