
<Wrapper::Card @header="List of Elements" @btnLabel="Add Element" @mode="list" @btnRoute="element-create">

  <Wrapper::List
    @modelName="element"
    @filterType={{this.filterType}}
    @sortOptions={{this.sortOptions}}
    @meta={{@model.meta}}
    @data={{@model.data}}
  />

</Wrapper::Card>